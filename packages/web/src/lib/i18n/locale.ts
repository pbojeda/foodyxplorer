// F-ADMIN-ANALYTICS-UI — i18n locale constant.
// Scope: admin panel only. /hablar, /login, and all other routes continue using
// hardcoded Spanish strings. This module is intentionally minimal (YAGNI).

export const LOCALE = 'es' as const;
export type SupportedLocale = typeof LOCALE;
