# QA-WEB-001 — Findings: Exhaustive Testing packages/web

**Date:** 2026-04-11
**Branch:** qa/QA-WEB-001-exhaustive-web-testing
**Baseline:** 26 suites, 263 tests | **Final:** 32 suites, 325 tests (+62 new)
**Lint:** Clean | **Typecheck:** Clean

---

## Severity Rubric

| Level | Definition | Criteria |
|-------|-----------|----------|
| **P0** | Release blocker | Crashes, data loss, or security vulnerability affecting all users. No workaround. |
| **P1** | Significant | Core flow broken or degraded for subset of users, or silent misconfiguration. Workaround may exist. |
| **P2** | Minor | UX polish, accessibility gap, code robustness. Does not block functionality. |

---

## Findings Table

| # | Sev | Bug ID | Finding | Flow | Reproducibility | User Impact | Workaround | Evidence |
|---|:---:|--------|---------|------|-----------------|-------------|------------|----------|
| 1 | **P1** | BUG-QA-001 | CSP `script-src` missing GA4 domain `googletagmanager.com` | Analytics | Always (when CSP enforced) | GA4 blocked, no analytics | Keep CSP in Report-Only | `csp.qa-web-001.test.ts` — asserts domain NOT present |
| 2 | **P1** | BUG-QA-002 | CSP `connect-src` missing GA4 endpoint `google-analytics.com` | Analytics | Always (when CSP enforced) | GA4 beacons blocked | Keep CSP in Report-Only | `csp.qa-web-001.test.ts` — asserts domain NOT present |
| 3 | **P1** | BUG-QA-003 | Route handler returns `{ error: 'CONFIG_ERROR' }` (string) instead of `{ error: { code, message } }` | Photo upload (misconfigured) | When API_KEY missing | Generic error shown, ops blind to config issue | None | `route.qa-web-001.test.ts` — asserts string format |
| 4 | **P2** | BUG-QA-004 | `ConfidenceBadge` crashes on unexpected `level` value | Estimation (edge) | If API returns unknown level | White screen (React crash) | API always returns known levels | `edge-cases.qa-web-001.test.tsx` — asserts TypeError |
| 5 | **P2** | BUG-QA-007 | `ErrorState` missing `role="alert"` for screen readers | Error state | Always on error | Screen readers don't announce error | Sighted users see the error | `a11y.qa-web-001.test.tsx` — asserts role absent |
| 6 | **P2** | BUG-QA-008 | No photo retry — `handleRetry` only re-sends text queries | Photo error | Always after photo error | User must re-select photo file manually | Re-select file | `gaps.qa-web-001.test.tsx` — asserts no sendPhotoAnalysis call |
| 7 | **P2** | BUG-QA-009 | No client-side query length pre-validation | Text query > 500 chars | Always with long input | 15s round-trip before error | Keep queries short | `gaps.qa-web-001.test.tsx` — asserts sendMessage IS called |
| 8 | **P2** | BUG-QA-011 | Actor ID persisted before response body validation | API response | On malformed 200 response | Actor ID overwritten with potentially incorrect value | Low probability | `apiClient.qa-web-001.test.ts` — asserts persistActorId called before parse |

| 9 | **P2** | BUG-QA-013 | `isConversationMessageResponse` / `isMenuAnalysisResponse` guards pass for `data: null` | API response | If backend returns `{ success: true, data: null }` | Client receives null, crashes on `response.data.intent` | Backend currently never returns this | `apiClient.qa-web-001.test.ts` — asserts no throw for null data |

### Code-Inspection Findings (no automated test)

| # | Sev | Bug ID | Finding | Recommendation |
|---|:---:|--------|---------|---------------|
| 9 | P2 | BUG-QA-010 | Route handler proxy has no upstream fetch timeout | Add `AbortSignal.timeout(60000)` to upstream fetch in `route.ts` |
| 10 | P2 | BUG-QA-012 | Misleading "no 'use client' needed" comment in ResultsArea | Remove or clarify comment |

### Exploratory (not verified)

| # | Sev | Bug ID | Finding | Verification needed |
|---|:---:|--------|---------|-------------------|
| 11 | P2 | BUG-QA-006 | `Permissions-Policy: camera=()` may block `<input capture="environment">` on Android | Test on real Android Chrome device |

---

## Recommendations

1. **Fix P1s before launch:** BUG-QA-001/002 (CSP GA4 domains) + BUG-QA-003 (route handler error format) — 3 tickets
2. **Fix P2-004 (ConfidenceBadge):** Add fallback `const config = BADGE_CONFIG[level] ?? BADGE_CONFIG['medium']` — prevents runtime crash
3. **Fix P2-007 (ErrorState a11y):** Add `role="alert"` to error container — one-line fix
4. **Defer P2-008/009:** Photo retry and client-side length check are UX enhancements, not blockers
5. **Verify P2-006 on Android:** Test `capture="environment"` with `Permissions-Policy: camera=()` on real device

---

*Generated by QA-WEB-001. All 325 tests passing.*
